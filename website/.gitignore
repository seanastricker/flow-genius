# Website deployment files - these are generated by the build process
# Don't commit the actual executable files as they are large (100-200MB each)

# Downloaded executable files
downloads/*.exe
downloads/*.dmg
downloads/*.AppImage
downloads/*.deb
downloads/*.blockmap

# Auto-updater files
downloads/latest.yml
downloads/latest-mac.yml
downloads/latest-linux.yml

# Generated deployment info
deployment-info.json

# Test server (will be regenerated)
test-server.js

# Temporary files
*.tmp
*.temp

# OS files
.DS_Store
Thumbs.db

# Only commit the HTML template and small assets
# The deployment script will populate the downloads folder 